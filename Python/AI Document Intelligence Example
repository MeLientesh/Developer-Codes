Relevant Tiny Code:

"""
      #The codes are private
      #The codes are simplified extraction
      #Production implementation is private
"""
def extract_invoice_text(pdf_path):
        pdf = fitz.open(pdf_path)

    text = "\n".join(
        page.get_text()
        for page in pdf
    )

    if not text.strip():
        text = run_ocr(pdf_path)

    return text

Validation: 

"""
      #The codes are private
      #The codes are simplified extraction
      #Production implementation is private
"""
def validate_total(subtotal, tax, total):
    calculated = subtotal + tax
    return abs(calculated - total) < 0.01
